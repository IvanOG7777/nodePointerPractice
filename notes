,get() method within the smart pointer is the actual struct of the smart pointer.

when we say for example node : firework.nodes, node is actually the wrapper around the pointer its of type std:: shared_ptr<FireworkNode>. .get() access the elements within FireworkNode